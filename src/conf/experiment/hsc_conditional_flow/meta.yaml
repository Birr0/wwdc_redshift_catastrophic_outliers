meta:
  authors:
  - BR
  data_id: 3
  data_name: hsc_embeddings
  description: Conditioninal flow matching on HSC images latent space.
  experiment_id: 5
  experiment_name: hsc_conditional_flow
  experiment_path: ${paths.data_dir}/${meta.data_name}/${meta.experiment_name}/
  version: 1.0
  data_path: ${paths.data_dir}/${meta.data_name}
  scratch_data_path: ${paths.scratch_data_dir}/${meta.data_name}/${hydra:job.id}